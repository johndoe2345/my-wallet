<%= link_to 'back', url_for(:back) %>
<br />
<h1>Add a card</h1>
<br />
<h3>All fields are required.</h3>
<% if @card.errors.full_messages.any? %>
	<ul>
		<% @card.errors.full_messages.each do |error_msg| %>
			<li><%= error_msg %></li>
		<% end %>
	</ul>
<% end %>
<%= render 'form' %>
<br />
